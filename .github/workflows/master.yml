name: Sync Issue Labels
on:
  issues:
    types: [opened]
jobs:
  dco-helper:
    runs-on: ubuntu-latest
    steps:
      - name: View context attributes
        uses: actions/github-script@v6
        with:
          script: console.log(context.payload.issue.title)
