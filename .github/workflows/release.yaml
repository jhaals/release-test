name: "Pull Request Labeler"
on:
- pull_request_target

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - name: install
      run: npm i -g zx
    - name: run dependabot script
      run: ls && ./bump.js
